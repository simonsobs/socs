UKKIDcontroller agent - installation and tutorial
=================================================

## Introduction

The UKKIDcontroller agent, ukkid_controller.py is an OCS agent that
communicates with hardware KRM RF System-on-chip (RFSoC) boards which
run the software and firmware for multi-tone readout of the UK Kinetic
Inductance Detectors (UKKIDs).

The RFSoC boards feature DACs, ADCs, FPGA fabric and a two-core ARM
processor running Ubuntu Linux. The tones to readout the KIDs are
generated by firmware pipelines running on the FPGA
fabric. Communication with these firmware pipelines is achieved by
python-based "readout server" software which runs on the RFSoCs ARM
cores. This server software takes IQ data which appears in the
firmwares/ARM cores shared memory and packages it into TCP/IP packets
which are sent to another computer which runs python software called
the "readout client". In practice this "readout client" software is a
python module, which can be imported into a python command line
interpreter and run interactively, or imported into other python
software or scripts. In our case the ukkid_controller.py imports the
readout_client module, which contains various methods for sending
instructions to and receiving data from the readout server saoftware running
on the RFSoc's ARM cores.

 The ukkid_controller.py has processes and tasks with effectively act
 as wrapper functions (with appropriate logging and error handling) for the
 functions provided by the readout_client.py module. These
 tasks/processes can e.g. request readout server configuration, start
 frequency scans, set tone frquencies and amplitudes and start
 streaming of IQ data. When streaming data for an astonomical
 observation, the ukkid_controller.py does not receive IQ TCP/IP data
 directly, but rather spawns separate streaming python streaming tasks
 (which also use the readout_client.py to communicate with the
 RFSoCs). The design is such that the ukkid_controller.py provides
 both sufficiently high-level tasks such that they can be used
 directly by telescope observers using the python "nextline" tool, and
 also low-level tasks for instrument characterisation and calibration,
 e.g. during in commissioning. The ukkid_controller.py also provides
 tools and interfaces (via the resonator_fitter module) for fitting
 and extracting resonator parameters from freqeuncy scans across the
 KID resonator arrays.


## Installation of prerequisities

 On a standard Ubuntu linux installation (at the time of writing
 Ubuntu 22.04 LTS) the following pre-requisities are required.

1. An Observatory Control System (OCS) installation. 

2. souk_readout_tools. This is the python code for the readout client
described above. (Note this repository also contains the code for the
readout server software that runs on the RFSoCs themselves). Use
branch dev/1.3.0 for compatiblity with the OCS system.

3. The ukkid_controller agent itself - i.e. everything within the
agents/UKKIDcontroller directory. This can be obained by checking out
the "ukkid_agent" branch of the official "socs" Simons Observatory
repository.

We will describe each of these in turn.

### OCS system

For this, one first needs to install docker (including docker compose). The instructions for this can be found here.

https://docs.docker.com/engine/install/ubuntu/#install-using-the-repository

The OCS system can be installed by following the instructions here.

https://ocs.readthedocs.io/en/main/user/installation.html

If installing for the first time, it is useful to read the
documentation about fake-data-agent and work through the "Create your
own agent tutorial".

After installing the OCS and running up the docker agents, one should
confirm that the docker agents are being launched correctly e.g. you
should get something like

```
leechj@aslxdaq35:~/ocs/ocs-site-configs$ sudo -E docker ps

CONTAINER ID   IMAGE                           COMMAND                  CREATED       STATUS       PORTS                                          NAMES
208029935b3d   simonsobs/ocs-crossbar:latest   "/ocs/run-crossbar.sh"   12 days ago   Up 12 days   8000/tcp, 8080/tcp, 127.0.0.1:8001->8001/tcp   ocs-site-configs-crossbar-1
d4643b7d7c48   grafana/grafana:latest          "/run.sh"                12 days ago   Up 12 days   127.0.0.1:3000->3000/tcp                       ocs-site-configs-grafana-1
037df03b0aa6   simonsobs/ocs:latest            "dumb-init ocs-agent…"   12 days ago   Up 12 days                                                  ocs-site-configs-ocs-influx-publisher-1
f422d459ce2e   simonsobs/ocs:latest            "dumb-init ocs-agent…"   12 days ago   Up 12 days                                                  ocs-site-configs-ocs-fake-data1-1
eb07461422d0   influxdb:1.7                    "/entrypoint.sh infl…"   12 days ago   Up 12 days   0.0.0.0:8086->8086/tcp, [::]:8086->8086/tcp    influxdb

```

The ocs-crossbar agent is essential for the client-agent
communication, so nothing will work without this. There is no
communication between the ukkidcontroller agent and the influxdB or
grafana agents, at the moment (although these could be made to
subscribe to the ukkidcontroller agents feed.)

### souk_readout_tools

One should follow the "quick-start" installation instructions here...

https://github.com/sr-cdf/souk_readout_tools

### Configuration of souk_readout_tools,

Overall configuration can be maintained either on the server side
(i.e. on the RFSoc linux filespace) or on the client side (on your
computer's filespace). The latter is easier (and how the OCS does
things, so follow the "create a config from the bundled template" from
the "quick-start" installation instructions.

```
from souk_readout_tools.config_utils import copy_template_config
copy_template_config('my_config.yaml', pipeline_id=0)
```

This will create a "my_config.yaml" file in the souk_readout_tools
directory.  You should edit it this to provide e.g. the IP addresses
you have set op for the RFSocs on your particular install, as
described in the souk_readout_tools documentation.

## OCS system to use the ukkid_controller agent.

Withing whereever you have put your ocs-site-configs directory (mine
is in ocs/ocs-site-configs directory).

Create a soft-link like...

default.yaml -> default_ukkid_non_docker.yaml

where default_ukkid_non_docker.yaml contains...


```
# Site configuration for a fake observatory.
hub:

  wamp_server: ws://localhost:8001/ws
  wamp_http: http://localhost:8001/call
  wamp_realm: test_realm
  address_root: observatory

hosts:

  aslxdaq35: {
    # Directory for logs.
    'log-dir': '/home/leechj/log/ocs/',

    # Agents running directly on the host machine
    # Note: We aren't going to run this Agent in the quickstart example,
    #       but this gives a good example of configuring an agent directly
    #       on the host
    'agent-instances': [
      {'agent-class': 'HostManager',
       'instance-id': 'hm-1'},
      {'agent-class': 'UKKIDController',
       'instance-id': 'UKKIDController1',
       'arguments': ['--mode', 'check_state',
                     '--kid_stream_id', 'ufm_kid1']},
      ]
  }

  aslxdaq35-docker: {
    # Address of crossbar within Docker (based on service name)
    'wamp_server': 'ws://crossbar:8001/ws',
    'wamp_http': 'http://crossbar:8001/call',

    # Agents running within Docker containers
    'agent-instances': [
      {'agent-class': 'InfluxDBAgent',
       'instance-id': 'influxagent',
       'arguments': ['--initial-state', 'record']},
      {'agent-class': 'FakeDataAgent',
       'instance-id': 'fake-data1',
       'arguments': ['--mode', 'acq',
                     '--num-channels', '16',
                     '--sample-rate', '4']},
    ]
  }
```

The other key configuration .yaml file in this directory is
docker-compose.yaml. Again I set up a softlink with


docker-compose.yaml -> docker-compose_my_barebones.yaml

and this is the content of mine

```

volumes:
  grafana-storage:
  influxdb-storage:

services:
  # --------------------------------------------------------------------------
  # Grafana for the live monitor.
  # --------------------------------------------------------------------------
  grafana:
    image: grafana/grafana:latest
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - grafana-storage:/var/lib/grafana

  # InfluxDB Backend for Grafana
  influxdb:
    image: influxdb:1.7
    container_name: "influxdb"
    restart: always
    ports:
      - "8086:8086"
    environment:
      - INFLUXDB_HTTP_LOG_ENABLED=false
    volumes:
      - influxdb-storage:/var/lib/influxdb

  # --------------------------------------------------------------------------
  # Crossbar Server
  # --------------------------------------------------------------------------
  crossbar:
    image: simonsobs/ocs-crossbar:latest
    ports:
      - "127.0.0.1:8001:8001" # expose for OCS
    environment:
         - PYTHONUNBUFFERED=1

  # --------------------------------------------------------------------------
  # OCS Components
  # --------------------------------------------------------------------------
  # Fake Data Agent for example housekeeping data
  ocs-fake-data1:
    image: simonsobs/ocs:latest
    hostname: aslxdaq35-docker
    environment:
      - INSTANCE_ID=fake-data1
      - LOGLEVEL=info
    volumes:
      - ${OCS_CONFIG_DIR}:/config:ro

  # InfluxDB Publisher
  ocs-influx-publisher:
    image: simonsobs/ocs:latest
    hostname: aslxdaq35-docker
    environment:
      - INSTANCE_ID=influxagent
    volumes:
      - ${OCS_CONFIG_DIR}:/config:ro
  # New barebones agent    
  ocs-barebones-agent:
    image: ocs-barebones-agent
    hostname: aslxdaq35-docker
    volumes:
     - ./:/config:ro
    environment:
     - INSTANCE_ID=barebones1
     - LOGLEVEL=info

```

Note there is no reference to the ukkid_controller agent, as this does
not run a dockerised OCS agent but as a "bare python" script via...

```
ocs-agent-cli --agent ukkid_controller.py --entrypoint main --instance-id UKKIDController1
```

## Running the UKKID controller agent without additional hardware (mocked mode)

## Running the UKKID controller agent with RFSoc hardware
